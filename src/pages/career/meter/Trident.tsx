import { MeterPage } from '../../../components/PageWrappers';
import { TRIDENT as meta } from './metas';

export default function Trident() {
  return (
    <MeterPage className='project-page'>
      <h1>{meta.title}</h1>
      <p>(To be completed)</p>
    </MeterPage>
  );
}
