import { DASHBOARD as meta } from './metas';

export default function Dashboard() {
  return (
    <div className="page project-page">
      <div className="page-overlay">
        <article className="page-content">
          <h1>{meta.title}</h1>
          <p>(To be completed)</p>
        </article>
      </div>
    </div>
  );
}
