import { MeterPage } from '../../../components/PageWrappers';
import { PROJECT_MANAGEMENT } from './metas';

export default function ProjectManagement() {
  return (
    <MeterPage className='project-page'>
      <h1>{PROJECT_MANAGEMENT.title}</h1>
      <p>(To be completed)</p>
    </MeterPage>
  );
}
