import { MeterPage } from '../../../components/PageWrappers';
import { NETWORK_DIAGNOSTICS } from './metas';

export default function NetworkDiagnostics() {
  return (
    <MeterPage className='project-page'>
      <h1>{NETWORK_DIAGNOSTICS.title}</h1>
      <p>(To be completed)</p>
    </MeterPage>
  );
}
