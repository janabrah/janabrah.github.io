import { ForwardPage } from '../../../../components/PageWrappers';
import { FORWARD_INVENTORY } from '../metas';

export default function ForwardInventory() {
  return (
    <ForwardPage className='project-page'>
      <h1>{FORWARD_INVENTORY.title}</h1>
      <p>(To be completed)</p>
    </ForwardPage>
  );
}
