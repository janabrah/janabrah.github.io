import { ForwardPage } from '../../../../components/PageWrappers';
import { IT_SYSTEMS } from '../metas';

export default function ITSystems() {
  return (
    <ForwardPage className='project-page'>
      <h1>{IT_SYSTEMS.title}</h1>
      <p>(To be completed)</p>
    </ForwardPage>
  );
}
